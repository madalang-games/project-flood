// AUTO-GENERATED by gen:data — do not edit
// Source: shared/datas/reward/reward_source.csv
#nullable enable
using System;
using System.Collections.Generic;
using System.IO;

namespace ProjectFlood.Generated.Data
{
    public sealed class RewardSource
    {
        public string source_id { get; set; } = "";
        public string source_type { get; set; } = "";
        public int reward_group_id { get; set; }
        public int version { get; set; }
        public string claim_policy { get; set; } = "";
        public int max_claims { get; set; }
        public string reset_timezone { get; set; } = "";
        public int reset_hour { get; set; }
        public string ui_surface { get; set; } = "";
        public bool is_enabled { get; set; }
    }

    public static class RewardSourceLoader
    {
        public static IReadOnlyList<RewardSource> LoadAll(string csvPath)
        {
            var result = new List<RewardSource>();
            if (!File.Exists(csvPath)) return result;
            using var reader = new StreamReader(csvPath);
            var headerLine = reader.ReadLine();
            if (headerLine == null) return result;
            var headers = SplitCsvLine(headerLine);
            var idx = new Dictionary<string, int>(StringComparer.Ordinal);
            for (int i = 0; i < headers.Length; i++) idx[headers[i]] = i;
            string? line;
            while ((line = reader.ReadLine()) is not null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var cols = SplitCsvLine(line);
                result.Add(new RewardSource
                {
                    source_id = idx.TryGetValue("source_id", out var i0) && i0 < cols.Length ? (cols[i0] ?? "") : "",
                    source_type = idx.TryGetValue("source_type", out var i1) && i1 < cols.Length ? (cols[i1] ?? "") : "",
                    reward_group_id = idx.TryGetValue("reward_group_id", out var i2) && i2 < cols.Length && !string.IsNullOrEmpty(cols[i2]) ? int.Parse(cols[i2]) : default,
                    version = idx.TryGetValue("version", out var i3) && i3 < cols.Length && !string.IsNullOrEmpty(cols[i3]) ? int.Parse(cols[i3]) : default,
                    claim_policy = idx.TryGetValue("claim_policy", out var i4) && i4 < cols.Length ? (cols[i4] ?? "") : "",
                    max_claims = idx.TryGetValue("max_claims", out var i5) && i5 < cols.Length && !string.IsNullOrEmpty(cols[i5]) ? int.Parse(cols[i5]) : default,
                    reset_timezone = idx.TryGetValue("reset_timezone", out var i6) && i6 < cols.Length ? (cols[i6] ?? "") : "",
                    reset_hour = idx.TryGetValue("reset_hour", out var i7) && i7 < cols.Length && !string.IsNullOrEmpty(cols[i7]) ? int.Parse(cols[i7]) : default,
                    ui_surface = idx.TryGetValue("ui_surface", out var i8) && i8 < cols.Length ? (cols[i8] ?? "") : "",
                    is_enabled = idx.TryGetValue("is_enabled", out var i9) && i9 < cols.Length && cols[i9] == "true",
                });
            }
            return result;
        }

        public static IReadOnlyDictionary<string, RewardSource> LoadAsDict(string csvPath)
            => LoadAll(csvPath).ToDictionary(r => r.source_id);

        private static string[] SplitCsvLine(string line)
        {
            var result = new List<string>();
            int i = 0;
            while (true)
            {
                if (i < line.Length && line[i] == '"')
                {
                    i++;
                    var sb = new System.Text.StringBuilder();
                    while (i < line.Length)
                    {
                        if (line[i] == '"')
                        {
                            if (i + 1 < line.Length && line[i + 1] == '"') { sb.Append('"'); i += 2; }
                            else { i++; break; }
                        }
                        else sb.Append(line[i++]);
                    }
                    result.Add(sb.ToString());
                }
                else
                {
                    int start = i;
                    while (i < line.Length && line[i] != ',') i++;
                    result.Add(line.Substring(start, i - start));
                }
                if (i >= line.Length) break;
                i++;
            }
            return result.ToArray();
        }
    }
}