// AUTO-GENERATED by gen:data — do not edit
// Source: shared/datas/reward/reward_item.csv
#nullable enable
using System;
using System.Collections.Generic;
using System.IO;

namespace ProjectFlood.Generated.Data
{
    public sealed class RewardItem
    {
        public int reward_group_id { get; set; }
        public int version { get; set; }
        public int sort_order { get; set; }
        public string reward_type { get; set; } = "";
        public int target_id { get; set; }
        public int amount { get; set; }
        public int duration_seconds { get; set; }
        public string stack_policy { get; set; } = "";
    }

    public static class RewardItemLoader
    {
        public static IReadOnlyList<RewardItem> LoadAll(string csvPath)
        {
            var result = new List<RewardItem>();
            if (!File.Exists(csvPath)) return result;
            using var reader = new StreamReader(csvPath);
            var headerLine = reader.ReadLine();
            if (headerLine == null) return result;
            var headers = SplitCsvLine(headerLine);
            var idx = new Dictionary<string, int>(StringComparer.Ordinal);
            for (int i = 0; i < headers.Length; i++) idx[headers[i]] = i;
            string? line;
            while ((line = reader.ReadLine()) is not null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var cols = SplitCsvLine(line);
                result.Add(new RewardItem
                {
                    reward_group_id = idx.TryGetValue("reward_group_id", out var i0) && i0 < cols.Length && !string.IsNullOrEmpty(cols[i0]) ? int.Parse(cols[i0]) : default,
                    version = idx.TryGetValue("version", out var i1) && i1 < cols.Length && !string.IsNullOrEmpty(cols[i1]) ? int.Parse(cols[i1]) : default,
                    sort_order = idx.TryGetValue("sort_order", out var i2) && i2 < cols.Length && !string.IsNullOrEmpty(cols[i2]) ? int.Parse(cols[i2]) : default,
                    reward_type = idx.TryGetValue("reward_type", out var i3) && i3 < cols.Length ? (cols[i3] ?? "") : "",
                    target_id = idx.TryGetValue("target_id", out var i4) && i4 < cols.Length && !string.IsNullOrEmpty(cols[i4]) ? int.Parse(cols[i4]) : default,
                    amount = idx.TryGetValue("amount", out var i5) && i5 < cols.Length && !string.IsNullOrEmpty(cols[i5]) ? int.Parse(cols[i5]) : default,
                    duration_seconds = idx.TryGetValue("duration_seconds", out var i6) && i6 < cols.Length && !string.IsNullOrEmpty(cols[i6]) ? int.Parse(cols[i6]) : default,
                    stack_policy = idx.TryGetValue("stack_policy", out var i7) && i7 < cols.Length ? (cols[i7] ?? "") : "",
                });
            }
            return result;
        }

        private static string[] SplitCsvLine(string line)
        {
            var result = new List<string>();
            int i = 0;
            while (true)
            {
                if (i < line.Length && line[i] == '"')
                {
                    i++;
                    var sb = new System.Text.StringBuilder();
                    while (i < line.Length)
                    {
                        if (line[i] == '"')
                        {
                            if (i + 1 < line.Length && line[i + 1] == '"') { sb.Append('"'); i += 2; }
                            else { i++; break; }
                        }
                        else sb.Append(line[i++]);
                    }
                    result.Add(sb.ToString());
                }
                else
                {
                    int start = i;
                    while (i < line.Length && line[i] != ',') i++;
                    result.Add(line.Substring(start, i - start));
                }
                if (i >= line.Length) break;
                i++;
            }
            return result.ToArray();
        }
    }
}