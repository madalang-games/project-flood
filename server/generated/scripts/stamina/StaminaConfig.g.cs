// AUTO-GENERATED by gen:data — do not edit
// Source: shared/datas/stamina/stamina_config.csv
#nullable enable
using System;
using System.Collections.Generic;
using System.IO;

namespace ProjectFlood.Generated.Data
{
    public sealed class StaminaConfig
    {
        public string config_id { get; set; } = "";
        public int max_life { get; set; }
        public int regen_seconds { get; set; }
        public int attempt_timeout_seconds { get; set; }
        public int max_revive_per_attempt { get; set; }
        public int revive_turn_1 { get; set; }
        public int revive_turn_2 { get; set; }
        public int revive_turn_3 { get; set; }
        public int ad_life_reward_amount { get; set; }
        public string daily_reset_timezone { get; set; } = "";
        public int daily_reset_hour { get; set; }
        public string default_unlimited_stack_policy { get; set; } = "";
    }

    public static class StaminaConfigLoader
    {
        public static IReadOnlyList<StaminaConfig> LoadAll(string csvPath)
        {
            var result = new List<StaminaConfig>();
            if (!File.Exists(csvPath)) return result;
            using var reader = new StreamReader(csvPath);
            var headerLine = reader.ReadLine();
            if (headerLine == null) return result;
            var headers = SplitCsvLine(headerLine);
            var idx = new Dictionary<string, int>(StringComparer.Ordinal);
            for (int i = 0; i < headers.Length; i++) idx[headers[i]] = i;
            string? line;
            while ((line = reader.ReadLine()) is not null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var cols = SplitCsvLine(line);
                result.Add(new StaminaConfig
                {
                    config_id = idx.TryGetValue("config_id", out var i0) && i0 < cols.Length ? (cols[i0] ?? "") : "",
                    max_life = idx.TryGetValue("max_life", out var i1) && i1 < cols.Length && !string.IsNullOrEmpty(cols[i1]) ? int.Parse(cols[i1]) : default,
                    regen_seconds = idx.TryGetValue("regen_seconds", out var i2) && i2 < cols.Length && !string.IsNullOrEmpty(cols[i2]) ? int.Parse(cols[i2]) : default,
                    attempt_timeout_seconds = idx.TryGetValue("attempt_timeout_seconds", out var i3) && i3 < cols.Length && !string.IsNullOrEmpty(cols[i3]) ? int.Parse(cols[i3]) : default,
                    max_revive_per_attempt = idx.TryGetValue("max_revive_per_attempt", out var i4) && i4 < cols.Length && !string.IsNullOrEmpty(cols[i4]) ? int.Parse(cols[i4]) : default,
                    revive_turn_1 = idx.TryGetValue("revive_turn_1", out var i5) && i5 < cols.Length && !string.IsNullOrEmpty(cols[i5]) ? int.Parse(cols[i5]) : default,
                    revive_turn_2 = idx.TryGetValue("revive_turn_2", out var i6) && i6 < cols.Length && !string.IsNullOrEmpty(cols[i6]) ? int.Parse(cols[i6]) : default,
                    revive_turn_3 = idx.TryGetValue("revive_turn_3", out var i7) && i7 < cols.Length && !string.IsNullOrEmpty(cols[i7]) ? int.Parse(cols[i7]) : default,
                    ad_life_reward_amount = idx.TryGetValue("ad_life_reward_amount", out var i8) && i8 < cols.Length && !string.IsNullOrEmpty(cols[i8]) ? int.Parse(cols[i8]) : default,
                    daily_reset_timezone = idx.TryGetValue("daily_reset_timezone", out var i9) && i9 < cols.Length ? (cols[i9] ?? "") : "",
                    daily_reset_hour = idx.TryGetValue("daily_reset_hour", out var i10) && i10 < cols.Length && !string.IsNullOrEmpty(cols[i10]) ? int.Parse(cols[i10]) : default,
                    default_unlimited_stack_policy = idx.TryGetValue("default_unlimited_stack_policy", out var i11) && i11 < cols.Length ? (cols[i11] ?? "") : "",
                });
            }
            return result;
        }

        public static IReadOnlyDictionary<string, StaminaConfig> LoadAsDict(string csvPath)
            => LoadAll(csvPath).ToDictionary(r => r.config_id);

        private static string[] SplitCsvLine(string line)
        {
            var result = new List<string>();
            int i = 0;
            while (true)
            {
                if (i < line.Length && line[i] == '"')
                {
                    i++;
                    var sb = new System.Text.StringBuilder();
                    while (i < line.Length)
                    {
                        if (line[i] == '"')
                        {
                            if (i + 1 < line.Length && line[i + 1] == '"') { sb.Append('"'); i += 2; }
                            else { i++; break; }
                        }
                        else sb.Append(line[i++]);
                    }
                    result.Add(sb.ToString());
                }
                else
                {
                    int start = i;
                    while (i < line.Length && line[i] != ',') i++;
                    result.Add(line.Substring(start, i - start));
                }
                if (i >= line.Length) break;
                i++;
            }
            return result.ToArray();
        }
    }
}