# Scripts/Localization — Font Localization Config

Namespace: `Game.Localization`

## Files
| file | class | role |
|------|-------|------|
| `FontLocalizationConfig.cs` | `FontLocalizationConfig` | ScriptableObject; maps Language enum → TMP_FontAsset (primary font per language) |

## Symbols
| symbol | kind | note |
|--------|------|------|
| `FontLocalizationConfig.GetFont(Language)` | method | Returns TMP_FontAsset for language; null if not configured |
| `FontLocalizationConfig.Entry` | struct | `Language language`, `TMP_FontAsset font` |

## Asset Location
`Assets/Resources/Localization/FontLocalizationConfig.asset`
Generated by: `Tools/Localization/Generate Font Config from subset_tool/config.json`

## Font Assignment (from config.json)
| language | primary font | fallback |
|----------|-------------|---------|
| EN, ES, FR, DE, PT, RU, TH, IT, TR, ID | Silver SDF | unifont SDF (set on asset) |
| KO | Galmuri11 SDF | unifont SDF (set on asset) |
| ZH_CN, ZH_TW, JA, AR | unifont SDF | — |

## Rules
- NEVER manually edit the .asset — regenerate via the menu item
- Fallback fonts are set on Silver SDF and Galmuri11 SDF assets by the generator (not per-text)

## Cross-refs
- Consumed by: `Game.Services.LocalizationService.GetFont()`
- Depends on: `tools/subset_tool/config.json`, TMP SDF font assets in `Assets/TextMesh Pro/Fonts/`
