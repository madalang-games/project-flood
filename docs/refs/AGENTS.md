# docs/refs — Platform Dependency References

External platform contract docs consumed by this game project.

## Files
| file | role |
|------|------|
| `platform-auth.md` | Auth service contracts: JWT format, JWKS endpoint, token refresh, mock auth |

## Rules
- These docs describe what platform provides — do not modify platform behavior from here
- Keep in sync with `platform/docs/refs/` when platform contracts change
